% Programa para reproducir una señal dada.
% tratare de llamar a esta funcion siempre que sea necesario
% Por el momento esta muy sencilla, pero se podra hacer mas compleja,
% tratar de ajustarla a cualquier caso posible.

function senal(f,fs)
%disp('Escribe la frecuencia del tono',f);
%disp('Escribe la frecuencia de muestreo',fs)
%disp('Escribe la funcion',y)
t=0:1/fs:1;
y=sin(2*pi*t*f);
plot(t,y);
sound